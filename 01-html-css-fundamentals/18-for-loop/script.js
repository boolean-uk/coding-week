// Simple loop using con console.log
for (let i = 0; i < 10; i++) {
    console.log("Count:" + i);
}

// Iterazione array
const names = ['Edward', 'Nathan', 'Jules'];
for (let i = 0; i < names.length; i++) {
    console.log(names[i]);
}
